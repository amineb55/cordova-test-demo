# Copy this file to `secrets.properties` (git-ignored) and fill in the values,
# or provide them as environment variables of the same name.

# OAuth 2.0 *Web* client id from the Firebase / Google Cloud console.
# Required for Google sign-in via Credential Manager.
GOOGLE_WEB_CLIENT_ID=YOUR_WEB_CLIENT_ID.apps.googleusercontent.com
